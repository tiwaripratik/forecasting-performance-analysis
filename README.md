# Dataset Project

Minimal repository for the dataset in this folder. Add a short description of the project here.

Contents:
- `notebook.ipynb` — analysis notebook
- `price_data.csv` / `price_data.txt` — raw data files

How to use:
1. Activate the virtual environment: `source venv/bin/activate`
2. Open the notebook: `jupyter notebook notebook.ipynb`
